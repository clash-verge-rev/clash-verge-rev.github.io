{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "serve",
      "type": "node-terminal",
      "request": "launch",
      "command": "mkdocs serve",
      "cwd": "${workspaceFolder}"
    }
  ]
}
